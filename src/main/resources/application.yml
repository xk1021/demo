server:
  port: 8080

spring:
  datasource:
    #   数据源基本配置
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/curd
    # 主要数据源
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  configuration:
    # 开启驼峰
    map-underscore-to-camel-case: true
  mapper-locations:  classpath:mapper/*.xml
